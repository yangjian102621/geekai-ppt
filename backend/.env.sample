# =============================================================================
# 后端环境变量示例
# 使用前请复制此文件为 .env 并填写真实值：cp .env.sample .env
# =============================================================================

# -----------------------------------------------------------------------------
# API 密钥（必填）
# -----------------------------------------------------------------------------
# 用于调用大模型与图像生成接口的 API Key。
# 可从 GeekAI (https://geekai.pro) 获取。
# 示例格式：sk-or-v1-xxxxxxxx 或 sk-xxxxxxxx
API_KEY=

# -----------------------------------------------------------------------------
# API 基础地址（可选，有默认值）
# -----------------------------------------------------------------------------
# 大模型与图像接口的 Base URL。默认为 https://api.geekai.pro。
# 如需使用其他服务，可改为任意兼容 Gemini API 协议的中转/官方地址。
BASE_URL=https://api.geekai.pro

# -----------------------------------------------------------------------------
# 模型名称配置（可选，有默认值）
# -----------------------------------------------------------------------------
# 规划/逻辑用模型，用于 PPT 大纲与提示词生成。
MODEL_LOGIC=gemini-3-pro-preview

# 图像生成用模型，用于生成幻灯片配图。
MODEL_IMAGE=gemini-3-pro-image-preview

# -----------------------------------------------------------------------------
# 服务端口（可选，默认 8002）
# -----------------------------------------------------------------------------
PORT=8002
