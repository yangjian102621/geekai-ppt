# ============================================================================
# GeekAI-PPT Frontend Environment Variables
# ============================================================================
# Copy this file to .env.development or .env.production and fill in your values.
# DO NOT commit the actual .env files to version control.
# ============================================================================

# Development/Proxy Settings
VITE_USE_PROXY=true          # Enable API proxy for development

# User Credentials (Demo/Test Users)
VITE_USER=your-phone-number      # Default user phone number
VITE_PASS=your-password           # Default user password
VITE_ADMIN_USER=admin             # Admin username
VITE_ADMIN_PASS=admin-password    # Admin password

# Application Configuration
VITE_APP_KEY_PREFIX=GEEK_DEV_     # Prefix for API key storage
VITE_TITLE=GeekAI-PPT             # Application title
VITE_LOGO=/images/logo.png        # Logo path
VITE_APP_VERSION=v1.0.0           # Application version
VITE_APP_GITHUB_URL=https://github.com/yangjian102621/geekai-ppt  # GitHub repository URL

# API Configuration
VITE_API_BASE_URL=/api        # Base API endpoint (use /api for proxy, or full URL like http://localhost:8002)

# ============================================================================
# Additional Notes:
# - For production deployment, set VITE_USE_PROXY=false and use absolute API URLs
# - Model provider and Gemini endpoint (e.g. https://api.geekai.pro) are configured on the backend via BASE_URL/API_KEY
# - You can also use any other service compatible with the Gemini API protocol
# - Keep sensitive credentials secure and never commit to version control
# - All environment variables prefixed with VITE_ are accessible in the frontend code
# ============================================================================
